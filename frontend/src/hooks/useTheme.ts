/**
 * useTheme — re-exported from ThemeContext for backward compatibility.
 * All components that import useTheme from this file will automatically
 * use the shared global ThemeContext state.
 */
export { useTheme } from '../context/ThemeContext';
